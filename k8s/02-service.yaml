apiVersion: v1
kind: Service
metadata:
  name: llm-serving
  namespace: llm-serving
spec:
  selector:
    app: llm-serving
  ports:
    - name: gateway
      port: 8000
      targetPort: 8000
    - name: ollama
      port: 11434
      targetPort: 11434
